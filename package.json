{
  "name": "msgbits",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "infra": "docker compose -f docker-compose.infra.yml up -d",
    "infra:down": "docker compose -f docker-compose.infra.yml down",
    "dev": "pnpm infra && turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
